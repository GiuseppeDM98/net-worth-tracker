/**
 * Class strings shared by the shell's surfaces (Sidebar, SecondaryMenuDrawer) so the account
 * block reads the same on desktop and in the phone drawer.
 */

/**
 * The account avatar: initials on the sidebar's own accent surface. A square of `--primary`
 * was the brightest element on the screen and competed with the page's verdict.
 */
export const SIDEBAR_AVATAR_CLASS =
  'flex size-7 shrink-0 items-center justify-center rounded-md bg-sidebar-accent text-[11px] font-semibold text-sidebar-accent-foreground';
