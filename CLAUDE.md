# CLAUDE.md - Net Worth Tracker (Lean)

## Project Overview
Next.js app for Italian investors: net worth, assets, cashflow, dividends, performance metrics and long-term planning on Firebase.

**This file is the INDEX**: "what it is + where it lives", nothing more — keep it well under 20.000 characters; it is injected into every turn. The *how* and the gotchas live in **AGENTS.md**; the aesthetic spec in **DESIGN.md**; env/emulators/Playwright in **SETUP.md**; users and positioning in **PRODUCT.md**. **Session rules and the guided-verification protocol are in [WORKFLOW.md](WORKFLOW.md); read it before starting.**

> **Language**: this file and AGENTS.md are in English. Italian is reserved for user-facing UI text. Page and feature names stay Italian, because they are the labels the product shows: Panoramica, Patrimonio, Cashflow, Analisi, Rendimenti, Allocazione, Storico, Previdenza, Impostazioni.

## Current Status
- Stack: Next.js 16, React 19, TypeScript 5, Tailwind v4, Firebase, Vitest, Framer Motion, Recharts, Yahoo Finance, Borsa Italiana scraping, Anthropic.
- `tsc` clean; **144 files / 3226 tests** green + **37 Playwright E2E specs** (40 green in one run incl. 3 auth setups). Run Vitest under `TZ=Europe/Rome` too — every date fixture sits at noon, which structurally hides timezone bugs.
- Latest (2026-08-31): **Cashflow › Divisione — how a household splits its shared spending.** An optional tab (`expenseSplitEnabled`) answering «quanto è costato in comune, e quanto resta a ciascuno?» on Tracciamento's period axis: verdict, the common pool by category, the split with the salaries behind it, and ONE TILE PER PERSON carrying `stipendio − quota comune − spese personali` — the figure that decides what reaches a personal investment account. (1) **One field carries it all**: `Expense.personalMemberId`, ABSENT = «in comune», so nothing is migrated and the normal case costs no interaction; it works on income too, which is where the shares come from (labor income attributed to a person). (2) **The share is never invented** — `resolveSplitBasis` returns `unavailable` naming who has no salary in the period, and every split-dependent figure goes `null` with it; without that guard whoever DID record a salary silently carries 100% of the pool. (3) The people are `FamilyMember` (already in Impostazioni → Famiglia, with the pension RAL): the same person, never a second list. (4) The monthly email carries the section over the SAME pure modules. Two enabling moves: `escapeHtml` now lives in `lib/server/emailHtml.ts` (a user-entered name reaches the email HTML), and the it-IT `formatPercentage`/`formatNumber` moved to `lib/utils/formatters.ts` as `formatPercentageIt`/`formatNumberIt` with `chartService` delegating — a narrative module read by the server cannot import `chartService`, which carries the client Firebase SDK. Also fixed, PRE-EXISTING: a URL naming an optional tab whose feature is off left Cashflow **blank** (reproduced on `?tab=cost-centers`); `effectiveTab` is now derived, covering both tabs. AGENTS → *Cashflow › Divisione*, *Settings — the FIVE places*, *Italian Localization*.

## Architecture Snapshot
- App Router; protected pages under `app/dashboard/*`.
- `lib/services/*` (service layer) → pure `lib/utils/*` → `lib/server/*` (server-only). React Query for caching/invalidation.
- Italy timezone helpers in `lib/utils/dateHelpers.ts`.
- Convention: extract logic into pure, tested `lib/utils`/`lib/services` functions; keep Firestore-coupled code thin.

## Key Features (Active)
One line per feature: what it is, then where it is described. *What the user sees* → README.md; *the rules and the traps* → AGENTS.md; *the aesthetic* → DESIGN.md.

- **Shared account**: a second user as full co-owner; **viewer** (`user.uid`) ≠ **owner** (`ownerId`, `useActiveAccount()`), grants in `account-access/{ownerUid}`, enforced in `firestore.rules` + `assertCanAccessAccount`. AGENTS → *Shared Account / Delegated Access*.
- **Demo mode**: public landing with auto-login; `useDemoMode()` gates every mutation. AGENTS → *Demo Mode*.
- **Shell**: compact `PageHeader` (one variant) · `PageTabBar` · `PageContainer width="wide"` + `TileGridSkeleton` for tile pages · sidebar with eyebrow group labels and the assistant as a route · bottom pill + «Altro» drawer. DESIGN → §5 Compact Page Header / Tile Grid; AGENTS → *Navigation*.
- **Panoramica**: a rule-generated verdict over a 12-column tile grid, one question per tile, on `GET /api/dashboard/overview`. README → *Portfolio Management*; AGENTS → *Panoramica and Dashboard Data Isolation*; DESIGN → §5 Page Verdict / Tile / Tile Grid.
- **Patrimonio**: the verdict's driver is an instrument; six tiles, Strumenti is the management table at the tile's cadence; a Δ is a unit-price variation; the Sottocategoria is optional. AGENTS → *Patrimonio*, *Asset Pricing, FX and Assets*.
- **AssetDialog + Asset trade ledger (Registro operazioni)**: 2-step create; BUY/SELL/ADJUSTMENT with cash settlement, Admin-API writes, the asset doc rebuilt by full replay. README → *Portfolio Management*; AGENTS → *Two-Step Create Dialogs*, *Asset Trade Ledger*.
- **Cashflow › Tracciamento**: «come sta andando il mese?» on one period axis; transfers are net-zero with atomic reconciliation; recurrence materialises real future rows. README → *Cashflow*; AGENTS → *Cashflow › Tracciamento*, *Expense Sign Convention*, *Recurring Series*.
- **Cashflow › Budget**: «sto rispettando il budget?», no axis, today's mark on every track, the ceiling historicised by the daily cron. AGENTS → *Cashflow › Budget*; DESIGN → Budget Track, Risk-vs-Fact.
- **Centri di Costo**: optional tab; «quanto sta costando il progetto?» with no axis — a project's cost is its whole cost. AGENTS → *Centri di Costo*; DESIGN → Whole-Cost Corollary.
- **Cashflow › Divisione**: optional tab; «quanto è costato in comune, e quanto resta a ciascuno?» on Tracciamento's axis — one field (`personalMemberId`, absent = in comune), shares from the period's attributed salaries, a tile per person, and a section in the monthly email. AGENTS → *Cashflow › Divisione*.
- **Expense CSV Import**: preview-first, one-tap undo by `importBatchId`. AGENTS → *Expense CSV Import*.
- **Analisi**: «dove vanno i soldi, e cosa è cambiato?» on the four-mode axis; a running year is compared full year against full year; the focused entity is a tile; the app's only Sankey. README → *Cashflow*; AGENTS → *Analisi — a verdict over tiles*, *Expense Grouping*, *Sankey: node identity*.
- **Dividendi**: «quanto rendono i miei flussi?»; received and announced are never one figure; BTP Italia coupons. README → *Dividends*; AGENTS → *Cashflow › Dividendi*, *Dividends and Coupons*.
- **Rendimenti**: «quanto rende il portafoglio, e rispetto a cosa?» on one axis, a configurable base, six benchmarks in EUR. README → *Performance Analytics*; AGENTS → *Rendimenti — measurement base / the measurement window / a verdict over tiles*.
- **Storico**: «come sono arrivato qui?» with no axis — wealth growth (contributions included), ONE pace for the verdict and the next doubling, per-instrument price/quantity attribution. README → *Historical Analysis*; AGENTS → *Storico — a verdict over tiles*, *History and Snapshot Baselines*.
- **Allocazione**: «sono allineato al piano, e cosa faccio con i prossimi soldi?» with no axis — the band is a scope in the Bilanciamento aside; five tiles + Dettaglio; `allocationRole` partitioned BEFORE `compareAllocations`; an unclassified holding is a row without a target; leverage as notional exposure. README → *Portfolio Management*; AGENTS → *Allocazione — a verdict over tiles*, *Allocation — allocationRole*, *Allocation — the two plans and the leverage engine*.
- **Previdenza**: «il fondo sta lavorando?» as a verdict per contributor (three causes, three numbers) over five tiles + «Dettaglio», the fiscal year beside the verdict; a price-1, frozen asset. README → *Portfolio Management*; AGENTS → *Fondo Pensione*, *Previdenza — a verdict over tiles*.
- **FIRE › Calcolatore + Coast FIRE + What If + Monte Carlo**: four verdicts over tiles — «quando?» (Traguardo · Base · Reddito passivo · Scenari, «Parametri»/«Dettaglio», the pension-lock switch in its tile); «posso smettere di versare?» (Traguardo · Afflussi · Scenari, «Ipotesi»/«Dettaglio», one `coastFireView.ts`); What If, tone from the delta in years, the event form a tile; Monte Carlo on the success rate, ONE run for three scenarios. Plus the pension bridge model. README → *FIRE Planning*, *Monte Carlo Simulations*; AGENTS → *FIRE, What If and Goals* + the four *FIRE › … — a verdict over tiles*.
- **Goal-Based Investing (FIRE › Obiettivi)**: «sono in rotta?» as a verdict over five tiles (Obiettivi · Traiettoria · Milestone · Allocazione derivata · Assegnazioni) with a «Dettaglio» disclosure; the Assistant proposes, only the user writes; SDK-free math in `goalMath.ts`. AGENTS → *FIRE › Obiettivi — a verdict over tiles*, *FIRE, What If and Goals*, *Assistant*.
- **Assistente AI**: «su quali numeri ragiona l'assistente?» — the verdict IS the context (a period's, in its tense, or the Panoramica's for a free question) on the period axis; the conversation as a tile beside a sticky companion of tiles (Patrimonio · Cashflow · Cosa sa di te); SSE streaming, five modes, gated web search, proactive memory, goals; flag `NEXT_PUBLIC_ASSISTANT_AI_ENABLED`, blocked in demo. AGENTS → *Assistant*, *Assistente — a verdict over tiles*.
- **Hall of Fame**: «quali sono stati i mesi e gli anni migliori?» senza asse — un record è una posizione; la classifica completa vive nel Dettaglio. README → *Other*; AGENTS → *Hall of Fame — a verdict over tiles*.
- **Impostazioni**: sei tab, nessun verdetto (è un form) ma la cadenza delle tessere; un solo Salva per pagina, e due tessere che dichiarano senza scrivere. AGENTS → *Impostazioni — tessere senza verdetto*, *Settings — the FIVE places*.
- **Accesso e Registrazione**: le due pagine pubbliche, una colonna da 420 con UNA tessera; il verdetto è la frase di prodotto ma è generato dallo stato (accesso · aperta · su invito · chiusa), la lettura della tessera è la status line del form, gli errori sono parole italiane e mai la stringa di Firebase. AGENTS → *Accesso e Registrazione*; DESIGN → §5 Status-Is-The-Reading.
- **Multi-theme · Periodic emails · PDF export**: README → *Other*; AGENTS → *Color Theme System*, *Periodic Emails*, *PDF Export*.

## Testing
- Vitest: `npx vitest run <file>`, `npm test -- <file>`, `npx tsc --noEmit`. New tests in `__tests__/`; prefer pure functions over Firestore-coupled code.
- **Phantom `tsc` errors** clustered in `e2e/` and `lib/utils/expenseImport.ts` after a branch switch: run `npm install` first (AGENTS → *Commands*).
- **Dev/test without production data**: Firebase Emulator Suite (`npm run emulators` + `emulators:seed` + `dev:emulator`), requires a JDK. SETUP.md → Step 6.
- **Browser (E2E)**: Playwright, `npm run test:e2e` with the emulators up (needs **Java ≥ 21**); app on :3100 with an isolated build dir. Accounts and fixtures: SETUP.md → Step 7; gotchas: AGENTS.md → *Browser-Driven E2E*.

## Data & Integrations
Firestore client + admin · Yahoo Finance (prices, benchmark history) · Borsa Italiana scraping (Italian bonds, dividends) · Frankfurter (FX) · FRED (`FRED_API_KEY`, series ECBDFR) · Anthropic (`claude-sonnet-5` analysis + assistant, `claude-haiku-4-5` extraction).

## Known Issues (Active)
- **FX** depends on Frankfurter with a 24h in-memory cache (no fallback on a cold instance). Pre-migration non-EUR assets without `currentPriceEur` show the native price as EUR until the first update; one with `autoUpdatePrice: false` never self-heals until re-saved.
- **Demo account** requires manual Firebase setup — README.md → Known Issues + the three `NEXT_PUBLIC_DEMO_*` vars.
- **Two deliberate dependency pins keep advisories open.** `firebase-admin` at `^13.6.0` (@14 pulls pure-ESM `jose@6` → `ERR_REQUIRE_ESM` on Vercel; 8 moderate `uuid` advisories stay) and `next` at `~16.2.12` (16.3.0 breaks Vercel at `onBuildComplete`; 2 HIGH libvips advisories via `sharp`, low exposure). **Unpin next and re-run `npm audit fix` once Vercel digests 16.3.x.**
- **Shared account setup** prerequisites (whitelist, guest registers first, rules deployed): SETUP.md → Step 5b.
- **YOC/Current Yield** exclude sold assets and are scoped to the current holding via `holdingStartDate`; a sell+rebuy inside one month, or a gap in months without `byAsset`, counts the prior holding's dividends against the new cost basis (an overstated YOC, never a regression).
- **Rendimenti before `byAsset`: correct denominator, wrong numerator** (2023-01 → 2025-10 on the real account): the basis step is removed, but the excluded assets' variation stays inside the measured return. Not reconstructible.
- **TWR monthly-bucket artifact (by design)**: an expense is neutralised only when the net-worth drop and the cash flow land in the same month; recording a purchase both as an expense and as an asset produces a phantom gain. Record balances in the month they belong to.
- **The Assistant's cashflow figures changed on 2026-07-29**; saved threads are prose and are not regenerated.
- **Chart slots 8-9 are still not theme-aware** (`useChartColors()` pads the last two from the static `CHART_COLORS`): slot 8 is Storico's synthetic «Previdenza» band and 9 is unused by the class palette, so nothing user-facing collides. Slots 0-7 became theme-aware on 2026-08-30 (`--chart-1..8` in all twelve blocks); **expect one repaint**: a cost center parked on identity slot 5 or 7 changes colour (5 rosa → oro, 7 arancio → rosa), slot 6 stays teal. AGENTS → *Color Theme System*.
- **Sign-colour CHIPS sit below AA, structurally** (`bg-positive/10 text-positive` washes the background with the text's hue: 15 of 24 combinations at 3.34–4.40:1; deliberately not fixed). `MonthlyReturnsHeatmap` fills its cells with the sign tokens at 30/55/85% (the figure is never printed in the cell, so the AA text floor does not apply). Emails and the PDF are a permanent token-drift surface (literal hexes outside the DOM).
- **A confirmed goal proposal can be confirmed again after a reload** (accepted for v1): reopening the thread re-parses the fenced block and a second press creates a SECOND goal.
- **A running year is the WHOLE calendar year on Tracciamento and Analisi**, so its figures include what is only scheduled; each verdict declares it with amount and horizon, each such row is chipped «In calendario» and drops its sign colour, and the months not started are drawn lighter. **«Da inizio anno» (YTD) is the other window** on both surfaces (`Period.kind = 'ytd'`, Analisi's fourth `PeriodMode`) — it runs to the END of today's month, not to today, so it carries scheduled rows too (three comments claimed otherwise until 2026-08-30). **On «Anno corrente» the delta compares twelve months against twelve** (`resolveComparisonScope` → `fullYear`, 2026-08-30), so the headline and the percentage share one window, at the cost of a delta biased downward as the year runs; YTD keeps `sameMonths`, the trailing savings history stays anchored to today, and Tracciamento's verdict and a category's Scheda still use «stessi mesi» for a running year — they name it, so they do not mislead. **The instalment dialog declares its cost once**: «Importo (euro)» is hidden with a plan on, «Importo totale» exists in both modes. Not extended to Panoramica, Storico, Budget or Centri di Costo. DESIGN → *The Scheduled-Is-Not-Spent Rule*.
- **Per-page blind spots** — the behaviours that look like bugs and are not — live in AGENTS.md → *Per-page blind spots*, one entry per page: Patrimonio · Allocazione · Tracciamento · Budget · Centri di Costo · Analisi · Dividendi · Rendimenti · Storico · Previdenza · Hall of Fame · Impostazioni · Accesso e Registrazione · Assistente · FIRE (Calcolatore · Coast FIRE · Monte Carlo · What If · Obiettivi). Moved there verbatim, to keep this file inside its 40.000-character budget.
- **Divisione's shares follow the PERIOD's salaries** (owner's call), so a month with a thirteenth salary or a bonus moves the percentage, and a month whose salary is not recorded yet has no shares at all — `resolveSplitBasis` says so by name instead of printing 100/0. Its blind spots: a running year's pool contains scheduled rows (the verdict declares them, like Tracciamento); income attributed to a person OUTSIDE the labor categories is neither pooled nor in the residual; a row whose `FamilyMember` was deleted sits in its own «Senza intestatario» bucket, never back in the pool; and there is **no reconciliation of who paid** — the paying account is deliberately never read. **The feature was shipped without an end-to-end run with the flag ON** (the guided verification stopped after phase A, owner's decision): the pure layer, the flag-off invariance, `tsc`, the suite and the build are proven; the writes of `personalMemberId` and the rendering are not.
- **The icon rail's 44px targets are measured at 1440 with a mouse**; no fixture covers a ≥1440px tablet in landscape.
- **The market digest's blind spots**: a position opened this month contributes 0 until next month; a pension fund counts only from `pensionReturnStartMonth`; hand-valued assets other than funds and real estate never show a market effect; real estate is measured gross of debt. Honest by construction.

## Key Files
Tests are not listed: every pure module has `__tests__/{module}.test.ts`, every page its `e2e/{page}*.spec.ts` where one exists.
- **Shell**: `app/dashboard/layout.tsx`, `components/layout/{Sidebar,BottomNavigation,SecondaryMenuDrawer,PageHeader,PageTabBar,PageTabs,PageContainer,ThemePicker,LogoutDialog}.tsx` + `shellStyles.ts` (`SIDEBAR_AVATAR_CLASS`), `components/ui/sidebar.tsx` (`SIDEBAR_WIDTH_ICON` = 3.5rem, icon-mode button size), `lib/constants/navigation.ts` (`primaryNav`/`analysisNav`/`planningNav`/`assistantNavItem`/`secondaryHrefs`), `lib/utils/navUtils.ts`; tile primitives `components/ui/{tile,narrative-text,ranked-rows,tile-grid-skeleton,page-verdict}.tsx`, `lib/utils/{narrative,tileGridSkeleton}.ts` (`narrative.ts` owns `Narrative`, `VerdictTone`, `PageVerdictModel`)
- **Patrimonio**: `app/dashboard/assets/page.tsx` (owns every dialog), `components/assets/{StrumentiTile,AssetRow,CashAccountDialog,AssetDialog,TransactionDialog,AssetMovementsDialog,TaxCalculatorModal,AssetSparkline}.tsx`, `components/assets/tiles/{LiquiditaTile,MovimentiTile,RendimentoTile}.tsx` (+ `PatrimonioTile`/`ComposizioneTile` reused from `components/dashboard/overview/` with `movers`/`countLine`/`eyebrow`/`footer` props); pure `lib/utils/{patrimonioNarrative,patrimonioSummary,assetPerformanceDeltas}.ts`; payload `topInstrumentMovers` via `computeTopInstrumentMovers` (`dashboardOverviewUtils.ts`)
- **Overview**: `app/dashboard/page.tsx`, `app/api/dashboard/overview/route.ts`, `lib/services/dashboardOverviewService.ts`, `lib/hooks/useDashboardOverview.ts`, `components/dashboard/overview/*` (`OverviewTile`/`NarrativeText`/`RankedRows`/`OverviewVerdict` re-exports or wrappers of the `components/ui` primitives, one file per tile; `PatrimonioTile` exports `resolveHeroValueClass`), `components/dashboard/{OverviewAnimatedCurrency,NetWorthSparkline,PeriodSelector}.tsx`, `lib/utils/{overviewNarrative,dashboardOverviewUtils,sparklinePeriod,savingsRateBadge}.ts`
- **Shared account**: `contexts/ActiveAccountContext.tsx`, `lib/services/accountAccessService.ts`, `app/api/account/members/route.ts`, `lib/server/apiAuth.ts`, `firestore.rules`, `components/settings/AccountSharingSection.tsx`; collection `account-access/{ownerUid}`
- **Shared primitives**: `components/ui/{composition-list,composition-bar,segmented-pill,drill-breadcrumb,responsive-modal,chart-hover}.tsx`, `lib/utils/compositionShading.ts`
- **Shared utils** (each the single source of its rule): `lib/utils/formatters.ts` · `metricColors.ts` (`getMetricValueColor`) · `assetPricing.ts` (`requiresManualPricing`) · `assetLiquidity.ts` (`suggestIsLiquid`) · `expenseTypeTransition.ts` (`needsSignFlip`/`crossesTransferBoundary`) · `firestoreData.ts` (`removeUndefinedDeep`) · `dateHelpers.ts` (`endOfMonthBound`, `getItalyDateIso`, `isItalyDayAfter`)
- **Rendimenti**: `app/dashboard/performance/page.tsx`, `components/performance/tiles/*` (seven tiles), `components/performance/{GrowthOfHundredChart,CapitalMarketChart,MonthlyReturnsHeatmap,PerformancePeriodPicker,PerformanceDettaglio,UnderwaterDrawdownChart,CustomDateRangeDialog,AIAnalysisDialog}.tsx`; pure `lib/utils/{performanceNarrative,performanceSummary}.ts` (words · numbers), `performanceBase.ts`, `drawdownSeries.ts`, `lib/services/performanceService.ts` (window, IRR, `CACHE_MATH_VERSION`), `cashFlowMap.ts`, `benchmarkPeriodReturn.ts`. Cache `performance-cache/{userId}`
- **Yields**: `lib/utils/yieldOnCost.ts` (`computeDividendYieldMetrics` — single source), consumed by Rendimenti and `app/api/dividends/stats/route.ts`
- **Assets**: `lib/services/assetService.ts` (`createAsset` ISIN reuse + `holdingStartDate`), `components/assets/*`, `types/assets.ts`, `lib/utils/{assetDisplay,assetDisplayClass}.ts`
- **Asset trade ledger**: engine `lib/utils/assetTransactionUtils.ts` + `types/assetTransactions.ts`; server `lib/server/{assetTransactionUseCase,tradeFxService}.ts` + `app/api/asset-transactions/*`; client `lib/services/assetTransactionService.ts` + `lib/hooks/useAssetTransactions.ts`; UI `components/assets/{TransactionDialog,AssetMovementsDialog}.tsx`. Collections `assetTransactions`/`assetTransactionsMeta`
- **Previdenza**: `types/pension.ts` + `pensionFundDetails`/`FamilyMember` (`types/assets.ts`); pure `lib/utils/{pensionSummary,pensionNarrative}.ts` (numbers · words) over `lib/utils/{pensionDeduction,pensionContributions,pensionReturn,pensionFire,pensionFamilyMembers}.ts`; `lib/services/pensionContributionService.ts` (`assertFundValueLivesInQuantity` = the price-1 guard); UI `app/dashboard/pension/page.tsx`, `components/pension/*` + `tiles/*`. Collection `pensionContributions`
- **E2E**: `playwright.config.ts`, `e2e/*.ts`, fixtures `scripts/{seedPensionE2E,seedAnalisiE2E,seedCoastFireE2E}.mts`; scripts `test:e2e`/`e2e:seed`/`e2e:seed:analisi`/`e2e:seed:coast`/`dev:e2e`
- **Allocazione / exposure**: `app/dashboard/allocation/page.tsx` (owns band · plan mode · amount), `components/allocation/tiles/{BilanciamentoTile,PianoTile,PerClasseTile,EsposizioneTile,PrevidenzaTile}.tsx`, `components/allocation/{AllocazioneDettaglio,BandToggle,BalanceRing,ActionChip,AllocationBreakdown,AllocationRow,TargetTick,PlanRow,InstrumentTradeList}.tsx`; pure `lib/utils/{allocazioneSummary,allocazioneNarrative}.ts` (numbers · words) over `lib/utils/{allocationUtils,leverageAwareAllocationUtils,assetExposureUtils}.ts` (`allocationUtils` also owns `ASSET_CLASS_SEQUENCE` + `assetClassSequenceIndex` = the one enumeration of the union, `ASSET_CLASS_LABELS` and `ASSET_CLASS_CHART_INDEX` = the one source of per-class chart slots), `lib/services/assetAllocationService.ts`, `lib/hooks/{useActionColors,usePortfolioExposure}.ts`; `lib/server/portfolioExposureService.ts`, `exposure-cache/{userId}`
- **Cashflow › Tracciamento**: `components/cashflow/ExpenseTrackingTab.tsx` (state, handlers, the grid; `applyListFilters` at module level), `components/cashflow/tiles/{CashflowPeriodoTile,FlowBarsChart,RisparmioTile,MovimentiTile}.tsx` (+ `CategoryTile` from the overview with `reading`), `components/cashflow/{TransactionFeed,CompactExpenseRow,MobileFiltersDrawer}.tsx`; pure `lib/utils/{tracciamentoSummary,cashflowNarrative}.ts`
- **Cashflow › Budget**: `components/cashflow/BudgetTab.tsx` (state, grid, the `cashflow:add-budget` listener), `components/cashflow/budget/tiles/*`, `components/cashflow/budget/{BudgetTrack,BudgetImpostazioni,BudgetItemDialog,budgetProgressStyle,useArmedDelete}.ts(x)`; pure `lib/utils/{budgetSummary,budgetNarrative,budgetUtils,budgetHistory,spendingProjection}.ts` (`spendingProjection` = the ONE month-end projection, shared with the Panoramica, Tracciamento and both budget emails; `budgetHistory` = the record builder + `resolveMonthCeilings`); `lib/hooks/{useBudgetConfig,useBudgetHistory}.ts`, `lib/services/budgetHistoryService.ts` (client), `lib/server/budgetHistoryService.ts` (cron phase 8); collections `budgets/{userId}`, `budgetHistory/{userId}/months/{YYYY-MM}`
- **Cashflow › Divisione**: `components/cashflow/ExpenseSplitTab.tsx` (orchestrator: period, grid), pure `lib/utils/{expenseSplitSummary,expenseSplitNarrative}.ts` (numbers · words — `resolveSplitBasis` = the ONE decision on whether a split exists, `allocateByShare` = the residual rule); the control lives in `components/expenses/ExpenseDialog.tsx`, the email section in `lib/server/monthlyEmailService.ts` over the same pure modules; `lib/server/emailHtml.ts` (`escapeHtml`, shared by both email services)
- **Cashflow / cost centers**: `app/dashboard/cashflow/page.tsx`, `components/cashflow/*`, `lib/utils/{costCenterUtils,cashflowTimeSeries,expenseImport,recurrenceDates}.ts` (`recurrenceDates` = the ONE source on recurrence dates, ceilings and eligible types), `lib/services/{budgetService,costCenterService,cashBalanceReconciliation,expenseImportService}.ts`
- **Centri di Costo**: `components/cashflow/{CostCentersTab,CostCenterDetail,CostCenterDialog,CostCenterErrorNotice}.tsx` (the tab owns query, dialog and the `cashflow:add-cost-center` listener), `components/cashflow/cost-centers/{CenterChip,ArchiviatiDisclosure}.tsx` + `cost-centers/tiles/*` (`CenterStackBars`); `costCenterStyles.ts` (`CHART_TICK_STYLE` — the canonical Recharts tick style); pure `lib/utils/{costCenterSummary,costCenterNarrative,costCenterUtils,costCenterColors}.ts` (numbers · words · compositions + unscoped dormancy · slot resolution), `lib/utils/spendingProjection.ts` (`projectWindowEndWithScheduled` = the ONE rule on any window); `lib/services/costCenterService.ts`
- **Analisi**: pure `lib/utils/{expenseGrouping,cashflowSankey,cashflowComposition,expenseCategoryMatching,comparisonDeltas,expenseEntityStats,entitySearch}.ts` + page layer `lib/utils/{analisiSummary,analisiNarrative}.ts`; UI `components/cashflow/AnalisiTab.tsx` (state, URL, grid, `handleEntitySelect`), `components/cashflow/analisi/*` (+ `tiles/*`), `components/cashflow/{EntityDossier,EntitySearch,ConfrontoAnnualeSection,CashflowSankeyChart,SavingsRateTrendSection,AndamentoStoricoSection}.tsx`, `components/cashflow/tiles/CashflowKpiTrio.tsx` (shared with Tracciamento)
- **Cashflow › Dividendi**: `components/dividends/DividendTrackingTab.tsx` (state, filters, the grid), `components/dividends/tiles/{IncassoNettoTile,AffidabilitaTile,RendimentoTile,PagatoriTile,PerAnnoTile,PagamentiTile,NetIncomeBars}.tsx`, `components/dividends/DividendiDettaglio.tsx`; pure `lib/utils/{dividendAnalytics,dividendiNarrative}.ts`; server block `lib/hooks/useDividendStats.ts` → `app/api/dividends/stats/route.ts` (payload type `DividendStatsPayload` in `types/dividend.ts`)
- **Dividendi (registro e cedole)**: `components/dividends/{DividendTable,DividendCalendar,CalendarDayCell,DividendDialog,DividendDetailsDialog,DividendRecordDetailsDialog,InflationRateDialog,ProvisionalCouponBanner}.tsx`, `lib/utils/couponUtils.ts`, `lib/services/couponScheduling.ts`, `types/dividend.ts`
- **Storico / snapshots**: `app/dashboard/history/page.tsx`, `components/history/tiles/*` (five tiles), `components/history/{StoricoDettaglio,SnapshotSearchDialog}.tsx`; pure `lib/utils/{storicoSummary,storicoNarrative}.ts` (numbers · words), `lib/utils/{snapshotAssetBreakdown,historyComposition}.ts` (`buildMonthAssetBreakdown`/`summarizeSelection` = the per-instrument reading; `historyComposition` = the ONE source on bands, normalization, residual and Δpp), `lib/services/{chartService,snapshotService}.ts` (`prepareDoublingTimeData`, `prepareSavingsVsInvestmentData*`, `prepareAssetClassHistoryData` returns EURO, never shares); `components/CreateManualSnapshotModal.tsx` over pure `lib/utils/manualSnapshotAmounts.ts` (fields generated from `ASSET_CLASS_SEQUENCE`); `components/ui/aside-toggle.tsx` (shared with Analisi); collection `monthly-snapshots`
- **Hall of Fame**: `app/dashboard/hall-of-fame/page.tsx`, `components/hall-of-fame/{RecordRows,RecordBars,NoteTrigger,HallOfFameDettaglio,HallOfFameNoteDialog,HallOfFameNoteViewDialog}.tsx` + `tiles/{RecordPatrimonioTile,RecordBoardTile,NoteTile}.tsx`; pure `lib/utils/{hallOfFameSummary,hallOfFameNarrative}.ts` (numeri · parole) sopra `lib/utils/hallOfFameRecords.ts` (**la ONE definizione di record e ranking**, condivisa con l'email e i due writer); `lib/constants/hallOfFame.ts` (`SECTION_LABELS` + le due liste di chiavi), `lib/services/hallOfFameService.ts` (client) e `hallOfFameService.server.ts` (Admin), `app/api/hall-of-fame/recalculate/route.ts`. Collezione `hall-of-fame/{userId}`
- **Benchmark**: `lib/constants/benchmarks.ts`, `app/api/benchmarks/*`, `lib/server/ecbRatesService.ts`; caches `benchmark-cache/*`, `fx-rate-cache/usd-eur`, `ecb-rate-cache/deposit-rate`
- **FIRE › Calcolatore**: `components/fire-simulations/FireCalculatorTab.tsx` (data flow; owns view · lock · form), `components/fire-simulations/tiles/{Traguardo,BaseDiCalcolo,RedditoPassivo,Scenari}Tile.tsx`, `components/fire-simulations/{FireParametri,FireDettaglio,FIREProjectionChart,FireFanChart,SettledValue}.tsx`; pure `lib/utils/{fireSummary,fireNarrative}.ts` (numbers · words) over `fireService`, `pensionUnlock`, `monteCarloService`; specs `e2e/fire*.spec.ts`
- **FIRE / goals**: `components/fire-simulations/*` (`FireFanChart` = the Ventaglio), `lib/services/{fireService,whatIfService,monteCarloService,goalService}.ts`, `lib/utils/{goalTrajectory,goalMath,pensionUnlock,monteCarloParams}.ts` (`pensionUnlock` = the single unlock resolution; `deriveMonteCarloAllocation` = the ONE allocation→MC-classes normalizer; `goalMath` = SDK-free math + `serializeGoalForFirestore`, the persistence allowlist)
- **FIRE › Obiettivi**: `components/fire-simulations/GoalBasedInvestingTab.tsx` (orchestrator: queries, the selection, the two dialogs), `components/goals/tiles/{Obiettivi,Traiettoria,Milestone,AllocazioneDerivata,Assegnazioni}Tile.tsx`, `components/goals/*` (`GoalsDettaglio`, the chart, the two dialogs, `goalVerdictMeta`); pure `lib/utils/{goalsSummary,goalsNarrative}.ts` (numbers · words) over `goalTrajectory.ts` and `goalMath.ts`
- **FIRE › What If**: `components/fire-simulations/WhatIfAnalysisTab.tsx` (orchestrator; owns the event, the form and the income selection), `components/fire-simulations/whatif/tiles/{PrimaDopo,Delta,Evento,Sensibilita}Tile.tsx`, `whatif/WhatIfProjectionChart.tsx` (the two walks overlaid), `whatif/incomeSelection.ts` (UI-only keys and sum); pure `lib/utils/{whatIfSummary,whatIfNarrative}.ts` (numbers · words) over `lib/services/whatIfService.ts` (`calculateWhatIfImpact` returns `projections`, `WHAT_IF_HORIZON_YEARS`) and `types/whatIf.ts` (`WhatIfBaseline.pensionBridge`)
- **FIRE › Monte Carlo**: `components/fire-simulations/MonteCarloTab.tsx` (orchestrator: form strings, scenarios, the run), `components/monte-carlo/tiles/{Probabilita,Distribuzione,ScenariConfronto,Parametri}Tile.tsx`, `components/monte-carlo/{MonteCarloFanChart,FinalValueBars,ScenarioOverlayChart,MonteCarloDettaglio}.tsx` (`SCENARIO_SLOT` = the one scenario → slot map); pure `lib/utils/{monteCarloSummary,monteCarloNarrative}.ts` over `monteCarloService.ts` and `monteCarloParams.ts`
- **Coast FIRE**: `components/fire-simulations/CoastFireTab.tsx` (orchestrator; owns the Ipotesi open state) + `coast/tiles/{CoastTraguardoTile,AfflussiTile,CoastScenariTile}.tsx` + `coast/{CoastIpotesi,CoastDettaglio}.tsx` + `CoastFireProjectionChart.tsx` (`fireNumberTarget` steps with the fund); pure `lib/utils/coastFireView.ts` (numbers AND words, **no math**: `summarizeCoast*`, `buildCoastVerdict`, `describe*`), form state `lib/hooks/useCoastFireSettingsDraft.ts`. Fixture `scripts/seedCoastFireE2E.mts`; specs `e2e/coast*.spec.ts`
- **Assistant**: `app/dashboard/assistant/page.tsx`, `components/assistant/AssistantPageClient.tsx` (orchestrator: the axis, the threads, the memory, the verdict), `components/assistant/tiles/{PatrimonioContestoTile,CashflowContestoTile,MemoriaTile}.tsx`, `components/assistant/{AssistantConversazioneTile,AssistantPromptRows,AssistantStreamingResponse,AssistantComposer,AssistantPeriodSelector,AssistantHeader,AssistantComeFunziona,AssistantSuggestionsBanner,AssistantSheets,AssistantThreadList,AssistantMemoryPanel,AssistantMemoryItemRow,AssistantPreferencesPopover}.tsx`; pure `lib/utils/assistantNarrative.ts` (words — the numbers are the bundle's) over `overviewNarrative.ts`; `app/api/ai/assistant/*`, `lib/server/assistant/*`; streaming `lib/hooks/useAssistantStreaming.ts`; period axis `lib/utils/assistantPeriodOptions.ts`; context `lib/services/assistantMonthContextService.ts` (4 period builders) over `lib/utils/expenseBreakdown.ts` (`buildCashflowBreakdown` — single aggregator)
- **Assistant — obiettivi**: pure `lib/server/assistant/goalEvaluation.ts`, I/O `goalEvaluationService.ts` (`evaluateActiveGoals`), extraction `memoryExtraction.ts`; `updatedAt` semantics in `store.ts` → `mergeMemoryItem`
- **Assistant — Goal-Based Investing**: `lib/server/goalData.ts`, `lib/utils/goalProposal.ts` (ONE zod schema for the fenced block and the route body), `app/api/goals/route.ts`, `components/assistant/GoalProposalCard.tsx` + the `pre` interception in `AssistantStreamingResponse.tsx`
- **Impostazioni / layout**: `app/dashboard/settings/page.tsx` (sei tab, ogni gruppo una tessera; owns `COLOR_THEME_SWATCHES`, `THEME_MODES`, `DeclarationRow`), `components/settings/{ExpenseImportSection,AccountSharingSection}.tsx` (ognuno rende la propria `Tile`); pure `lib/utils/settingsNarrative.ts` (le letture, **niente verdetto**), `lib/services/assetAllocationService.ts` (`calculateEquityPercentage` + le CINQUE sedi), `lib/utils/equityBondsAutoTargets.ts`, `components/layout/*`, `lib/constants/navigation.ts`
- **Accesso e Registrazione**: `app/login/page.tsx`, `app/register/page.tsx`, `components/auth/{AuthShell,AuthField,AuthStatusLine,PasswordRequirements,GoogleMark}.tsx` (`AuthShell` esporta anche `AuthDivider`); pure `lib/utils/authNarrative.ts` (verdetti, letture, `describeAuthError`, i due requisiti password); policy server `lib/server/registrationPolicy.ts` + `app/api/auth/check-registration/route.ts` (403 con `code`), sessione in `contexts/AuthContext.tsx` (`withCode`), guardia `components/ProtectedRoute.tsx` (invariata)
- **Server use case / email**: `lib/server/{assetAdminRepository,dividendUseCase,dividendProcessor,monthlyEmailService,weeklyBudgetEmailService,emailPeriodComparison}.ts`, `app/api/cron/monthly-snapshot/route.ts` (phases 2-7)
- **PDF export**: `lib/utils/pdfGenerator.tsx` → `lib/services/pdfDataService.ts` → `components/pdf/*`; `lib/utils/pdfTimeFilters.ts`; types `types/pdf.ts`

## Design Context
Propagation plan: **`docs/redesign-prompts.md`** — one ready-to-paste prompt per section (shell, every page/tab, auth, landing, dialogs, states, email/PDF) with the recommended model/effort and the closing checklist.

Authoritative aesthetic spec: **DESIGN.md** (Apple + Linear/Vercel + Trade Republic; form-follows-function) — hand-maintained, **never regenerate it**; its YAML frontmatter is the normative layer read by the impeccable detector, `.impeccable/design.json` is only the extensions sidecar. Product truth lives in **PRODUCT.md**. Principles: (1) data first, decoration never; (2) motion with a purpose; (3) density is a feature; (4) precision builds trust; (5) personality lives in the details.

**Last updated**: 2026-08-31 (DESIGN.md documents the "Verdict over Tiles" shape, the shell and the nineteen propagations, Patrimonio to Accesso e Registrazione, each with the rule it added (DESIGN.md §5, Table inside a Tile → **The Status-Is-The-Reading Rule**); superseded patterns stay marked. Cashflow › Divisione (2026-08-31) is the second optional tab and adds no new visual rule: it is the tile grid applied to a household's question, with the per-person tile as its only new shape. Impostazioni is the propagation that has NO verdict — a form measures nothing — and the one that added the read-only tile; Accesso e Registrazione is the one with a SINGLE tile and no grid, whose verdict is the product's promise rather than a reading of data. The Scheduled-Is-Not-Spent Rule (2026-08-28) is not a propagation but a revision of what a PERIOD means on Tracciamento and Analisi; on 2026-08-30 it gained its third clause — the scheduled amount is INSIDE the total, and the words have to say so — alongside the new **Untargeted-Row Rule**, the three theme-aware chart slots 6-8 in §2 (which stops carrying a second copy of the class→slot map), the article before a printed figure added to the Comma Rule («allo 0,1%»), and the comparison corollary rewritten — a delta is measured on the span its period covers.) History: `git log`.
